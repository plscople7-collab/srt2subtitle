【概要】

* 前提変更: 音声は話者ごとに分離済みとし、話者分離機能は初期スコープ外にする。
* 中核仕様: 「話者別音声ファイル群＋話者別サンプル`.exo`」を入力し、音声認識結果を各話者テンプレートへ流し込んだAviUtl用字幕`.exo`を生成する。
* 設計原則: 同じ処理は共通化し、話者ごとの差分は`.exo`テンプレートと設定データへ逃がす。音声認識・字幕分割・`.exo`解析・`.exo`生成・UIは責務分離する。
* 初期MVP: ローカルWebアプリ。ブラウザで音声とサンプル`.exo`を登録し、Pythonバックエンドで文字起こしと`.exo`生成を行う。
* 検証基準: 出力`.exo`をAviUtlへ読み込んだ際、各話者のフォント・色・縁取り・位置・レイヤーがサンプル通り再現され、字幕本文と開始終了フレームが音声に対応すること。

【詳細解説】
以下、仕様指示書に合わせて「要件・機能・挙動・データ・UI/API・実装・テスト・ADR」に分離して落とし込む。

# AI字幕EXO生成ツール 仕様指示書 v0.1

## 0. 全体概要

目的: 分離済みの話者別音声データを入力し、音声認識により字幕本文と時間情報を生成し、話者別サンプル`.exo`から抽出した見た目を適用して、AviUtl拡張編集へ読み込める字幕`.exo`を自動生成する。
対象: AviUtlで複数話者の字幕を作成するユーザー。
非目的: 初期版では話者分離、動画編集、音声補正、翻訳、字幕焼き込み、AviUtl本体操作の自動化は扱わない。
設計原則: 音声認識、字幕分割、話者設定、`.exo`テンプレート解析、`.exo`生成、UI表示を分離する。共通処理は一箇所へ集約し、話者ごとの差分は設定データとテンプレート`.exo`で管理する。
入力: 話者別音声ファイル、話者別サンプル`.exo`、プロジェクト設定。
出力: 統合字幕`.exo`、確認用`.srt`、確認用`.json`。

## 1. 要件定義

### REQ-001: 話者別音声入力

状態: accepted
内容: ユーザーは話者ごとに分離済みの音声ファイルを登録できなければならない。
理由: 初期版では話者分離を行わず、既に分かれている音声を前提に字幕化するため。
入力: `wav`, `mp3`, `m4a`, `aac`, `flac`のいずれか。
出力: 話者IDに紐づいた音声入力リスト。
制約: 1話者につき1ファイル以上を許可する。複数ファイル登録時は登録順に処理する。
エラー: 未対応形式の場合は`ERR-AUDIO-FORMAT-001`を返す。
依存: DATA-001, UI-001, BEH-001。
テスト: 対応形式5種の読み込み可否を確認する。

### REQ-002: 話者別EXOテンプレート入力

状態: accepted
内容: ユーザーは話者ごとに「字幕テスト」と書かれたサンプル`.exo`を登録できなければならない。
理由: AviUtl上で作った字幕デザインを、プログラム側で再現するため。
入力: 話者ID、サンプル`.exo`。
出力: 話者IDに紐づいた字幕テンプレート。
制約: サンプル`.exo`内に、本文が「字幕テスト」であるテキストオブジェクトを1個以上含むこと。
エラー: 対象テキストが見つからない場合は`ERR-EXO-TEMPLATE-001`を返す。対象テキストが複数あり自動選択できない場合は`ERR-EXO-TEMPLATE-002`を返す。
依存: DATA-002, BEH-004, IMPL-003。
テスト: フォント、色、縁取り、位置、レイヤーを持つ`.exo`からテンプレート抽出できること。

### REQ-003: 話者別字幕長指定

状態: accepted
内容: ユーザーは話者ごとに字幕の最大文字数、最大行数、最短表示時間、最長表示時間を指定できなければならない。
理由: 話者ごとに字幕の密度、話速、見た目を変える必要があるため。
入力: `max_chars`, `max_lines`, `min_duration_sec`, `max_duration_sec`。
出力: 話者別字幕分割設定。
制約: `max_chars`は1以上80以下、`max_lines`は1以上4以下、`min_duration_sec`は0.1以上、`max_duration_sec`は`min_duration_sec`以上10.0以下。
エラー: 範囲外の場合は`ERR-SUBTITLE-RULE-001`を返す。
依存: DATA-003, BEH-002。
テスト: 境界値1/80文字、1/4行、0.1/10.0秒を確認する。

### REQ-004: AviUtl用EXO出力

状態: accepted
内容: システムは全話者の字幕を1つの`.exo`として出力できなければならない。
理由: AviUtl拡張編集へ直接読み込める成果物を作るため。
入力: 字幕セグメント群、話者テンプレート群、プロジェクト設定。
出力: `output.exo`。
制約: フレームレート、解像度、音声設定はプロジェクト設定またはテンプレート`.exo`から決定する。
エラー: フレーム変換不能の場合は`ERR-EXO-FRAME-001`を返す。
依存: DATA-004, BEH-005, IMPL-004。
テスト: 出力`.exo`をAviUtlで読み込み、全字幕が時系列通り表示されること。

## 2. 機能仕様

### FUNC-001: プロジェクト作成

内容: 新規プロジェクトを作成し、fps、解像度、出力名、既定字幕設定を保持する。
入力: `fps`, `width`, `height`, `project_name`。
出力: プロジェクト設定。
制約: `fps`は24, 30, 60のいずれかを初期UI候補とする。直接入力では1以上240以下を許可する。
エラー: 数値不正時は`ERR-PROJECT-001`。

### FUNC-002: 話者登録

内容: 話者ID、表示名、音声ファイル、サンプル`.exo`、字幕長設定を登録する。
入力: 話者名、音声ファイル、サンプル`.exo`、字幕分割設定。
出力: 話者設定。
制約: 話者IDは内部で一意に生成する。表示名の重複は許可する。
エラー: 音声または`.exo`未登録の場合は`ERR-SPEAKER-001`。

### FUNC-003: 音声認識

内容: 話者別音声ファイルを文字起こしし、開始時刻、終了時刻、本文を持つ認識セグメントを生成する。
入力: 話者ID、音声ファイル。
出力: 認識セグメント群。
制約: 初期版では同一音声内に別話者が混在していても話者分離しない。全セグメントは入力音声に紐づく話者IDとして扱う。
エラー: 認識失敗時は`ERR-ASR-001`。

### FUNC-004: 字幕分割

内容: 認識セグメントを字幕表示に適した長さへ分割または結合する。
入力: 認識セグメント群、話者別字幕長設定。
出力: 字幕セグメント群。
制約: 分割は句読点、空白、文節境界、最大文字数、最大秒数の順で判定する。
エラー: 分割不能な長文は最大文字数を超過したまま`warning: WARN-SUBTITLE-LONG-001`を付与する。

### FUNC-005: EXOテンプレート解析

内容: サンプル`.exo`から「字幕テスト」を含むテキストオブジェクトと付随フィルタを抽出する。
入力: サンプル`.exo`。
出力: EXOテンプレート。
制約: テンプレート抽出では未知のフィルタを削除しない。オブジェクト単位で保持する。
エラー: テキストオブジェクト不在時は`ERR-EXO-TEMPLATE-001`。

### FUNC-006: EXO生成

内容: 字幕セグメントごとに話者テンプレートを複製し、開始フレーム、終了フレーム、本文、セクション番号を差し替える。
入力: 字幕セグメント群、EXOテンプレート群、プロジェクト設定。
出力: 統合`.exo`。
制約: セクション番号は出力時に0から連番で再採番する。
エラー: テンプレートと話者IDが対応しない場合は`ERR-EXO-SPEAKER-001`。

### FUNC-007: 確認用出力

内容: `.exo`とは別に、確認用`.srt`と`.json`を出力する。
入力: 字幕セグメント群。
出力: `output.srt`, `output.json`。
制約: `.srt`は字幕本文と時刻確認用であり、話者別フォント情報は保持しない。
エラー: 書き込み失敗時は`ERR-EXPORT-001`。

## 3. 挙動仕様

### BEH-001: 音声処理順序

前提: 話者A、話者Bの音声が登録済みである。
処理:

1. プロジェクト設定を検証する。
2. 各話者設定を検証する。
3. 話者ごとに音声認識を実行する。
4. 認識セグメントへ話者IDを付与する。
5. 話者別ルールで字幕分割する。
6. 全話者の字幕セグメントを開始時刻昇順で統合する。
7. 同時刻字幕は話者登録順で並べる。
8. `.exo`, `.srt`, `.json`を生成する。
   境界値: 開始時刻が同一の場合、終了時刻が短い字幕を先にする。開始時刻と終了時刻が完全一致する場合、話者登録順を採用する。

### BEH-002: 字幕分割規則

前提: 認識セグメントが本文と開始終了時刻を持つ。
処理:

1. 本文が`max_chars * max_lines`以内、かつ表示時間が`max_duration_sec`以内なら分割しない。
2. 表示時間が`max_duration_sec`を超える場合、句点`。`、読点`、`、疑問符`？`、感嘆符`！`を優先して分割する。
3. 句読点分割後も長い場合、文節境界候補で分割する。
4. 文節境界が取れない場合、`max_chars`を基準に強制分割する。
5. 分割後の各字幕が`min_duration_sec`未満の場合、隣接字幕との結合を試みる。
6. 結合により最大文字数または最大秒数を超える場合、短時間字幕のまま保持し、`WARN-SUBTITLE-SHORT-001`を付与する。
   禁止: 「自然に分割する」のような判定不能ルールを単独で仕様化しない。

### BEH-003: 改行規則

前提: 字幕本文が最大1行文字数を超える。
処理:

1. `max_lines=1`の場合、改行しない。
2. `max_lines>=2`の場合、1行あたりの文字数が均等に近くなる位置を候補にする。
3. 句読点直後、助詞直後、空白位置を優先候補にする。
4. 禁則対象文字が行頭または行末に来る場合、前後1文字以内で補正する。
5. 補正不能な場合は文字数均等を優先する。
   出力: 改行コードを含む字幕本文。

### BEH-004: EXOテンプレート抽出規則

前提: サンプル`.exo`が読み込まれている。
処理:

1. `.exo`をセクション単位で解析する。
2. テキストオブジェクトを探索する。
3. 本文復号後に「字幕テスト」と完全一致するオブジェクトを候補にする。
4. 候補が1個の場合、その親オブジェクトと付随フィルタ群をテンプレートとする。
5. 候補が0個の場合、`ERR-EXO-TEMPLATE-001`を返す。
6. 候補が2個以上の場合、UIで選択させるか、初期版では`ERR-EXO-TEMPLATE-002`を返す。
   保持対象: フォント、サイズ、文字色、影色、縁取り、座標、拡大率、透明度、回転、レイヤー、描画方式、未知フィルタ。

### BEH-005: フレーム変換規則

前提: 字幕セグメントが秒単位の開始終了時刻を持つ。
処理:

1. `start_frame = round(start_sec * fps) + 1`で開始フレームを算出する。
2. `end_frame = max(start_frame, round(end_sec * fps))`で終了フレームを算出する。
3. 前字幕と同一話者かつフレーム範囲が重複する場合、後続字幕の開始を前字幕終了+1へ補正する。
4. 異なる話者の重複は許可する。
   制約: fpsはプロジェクト全体で統一する。

## 4. データ仕様

### DATA-001: プロジェクト設定

形式: JSON。
内容:

```json
{
  "schema_version": "0.1",
  "project": {
    "name": "subtitle_project",
    "fps": 60,
    "width": 1920,
    "height": 1080,
    "audio_rate": 48000,
    "audio_ch": 2
  }
}
```

制約: `schema_version`は必須。将来互換のため削除しない。

### DATA-002: 話者設定

形式: JSON。
内容:

```json
{
  "speakers": [
    {
      "speaker_id": "spk_001",
      "display_name": "話者A",
      "audio_files": ["speaker_a.wav"],
      "template_exo": "speaker_a.exo",
      "subtitle_rule": {
        "max_chars_per_line": 18,
        "max_lines": 2,
        "min_duration_sec": 0.8,
        "max_duration_sec": 4.0
      }
    }
  ]
}
```

制約: `speaker_id`は内部一意。`display_name`は表示用であり一意性を要求しない。

### DATA-003: 認識セグメント

形式: JSON内部データ。
内容:

```json
{
  "speaker_id": "spk_001",
  "source_audio": "speaker_a.wav",
  "start_sec": 12.34,
  "end_sec": 15.67,
  "text": "これはテストです。",
  "confidence": 0.92
}
```

制約: `start_sec < end_sec`であること。`confidence`が取得不能な場合は`null`。

### DATA-004: 字幕セグメント

形式: JSON内部データ。
内容:

```json
{
  "subtitle_id": "sub_000001",
  "speaker_id": "spk_001",
  "start_sec": 12.34,
  "end_sec": 15.67,
  "start_frame": 741,
  "end_frame": 940,
  "text": "これはテストです。",
  "warnings": []
}
```

制約: `.exo`生成時は`start_frame`と`end_frame`を必須とする。

### DATA-005: EXOテンプレート

形式: 内部データ。
内容:

```json
{
  "speaker_id": "spk_001",
  "template_source": "speaker_a.exo",
  "object_sections": [
    "[0]",
    "[0.0]",
    "[0.1]"
  ],
  "text_section_key": "[0.0]",
  "placeholder_text": "字幕テスト",
  "raw_params": {}
}
```

制約: 未知パラメータは破棄しない。出力時に原則保持する。

## 5. UI仕様

### UI-001: プロジェクト設定画面

項目:

* プロジェクト名
* fps
* 解像度
* 出力ファイル名
  制約: fps未指定時は60。解像度未指定時は1920×1080。

### UI-002: 話者設定画面

項目:

* 話者追加ボタン
* 話者名
* 音声ファイル選択
* サンプル`.exo`選択
* 最大文字数/行
* 最大行数
* 最短表示秒数
* 最長表示秒数
* テンプレート検証結果
  挙動: `.exo`選択後、テンプレート抽出に成功した場合は、抽出したフォント名、サイズ、色、レイヤーを表示する。

### UI-003: 生成画面

項目:

* 文字起こし開始
* 字幕プレビュー
* 話者別表示フィルタ
* `.exo`出力
* `.srt`出力
* `.json`出力
  制約: 字幕本文は出力前に手修正できる。手修正後の本文を`.exo`生成に使用する。

## 6. API仕様

### API-001: プロジェクト作成

`POST /api/project/create`
入力:

```json
{
  "name": "subtitle_project",
  "fps": 60,
  "width": 1920,
  "height": 1080
}
```

出力:

```json
{
  "project_id": "prj_001"
}
```

### API-002: 話者登録

`POST /api/speakers`
入力: `multipart/form-data`。
項目:

* `project_id`
* `display_name`
* `audio_file`
* `template_exo`
* `subtitle_rule_json`
  出力:

```json
{
  "speaker_id": "spk_001",
  "template_status": "ok"
}
```

### API-003: 文字起こし実行

`POST /api/transcribe`
入力:

```json
{
  "project_id": "prj_001"
}
```

出力:

```json
{
  "segments": []
}
```

### API-004: EXO生成

`POST /api/export/exo`
入力:

```json
{
  "project_id": "prj_001",
  "segments": []
}
```

出力: `.exo`ファイル。

## 7. 実装仕様

### IMPL-001: 推奨構成

構成:

```text
frontend/
  index.html
  app.js
  style.css

backend/
  main.py
  transcribe.py
  subtitle_splitter.py
  exo_parser.py
  exo_writer.py
  project_store.py
  validators.py

data/
  projects/
```

理由: UI、音声認識、字幕分割、`.exo`処理、保存処理を分離し、変更理由を混線させないため。

### IMPL-002: 音声認識エンジン

初期候補:

* ローカル: `faster-whisper`
* API版: OpenAI音声認識API
  方針: 音声認識エンジンは差し替え可能なインターフェースにする。上位仕様では特定ライブラリへ依存しない。
  インターフェース:

```python
class Transcriber:
    def transcribe(self, audio_path: str) -> list[RecognizedSegment]:
        ...
```

### IMPL-003: EXO解析

方針: `.exo`を完全再構築せず、AviUtlが出力した構造を保持したまま必要箇所だけ差し替える。
理由: 未知フィルタ、追加効果、座標、縁取り、アニメーション設定を壊しにくくするため。
処理対象:

* 親オブジェクトの`start`, `end`, `layer`
* テキストフィルタの`text`
* セクション番号
* 必要時のみ全体設定の`length`

### IMPL-004: EXO文字列エンコード

方針: サンプル`.exo`内の文字列エンコード方式を検出し、同じ方式で置換文字列を書き込む。
理由: AviUtlの`.exo`は文字列格納方式の影響を受けるため。
制約: 初期版では、解析対象サンプルと同じ形式で出力する。未知形式の場合は`ERR-EXO-ENCODING-001`。

## 8. テスト仕様

### TEST-001: 話者別テンプレート再現

目的: サンプル`.exo`の見た目が出力`.exo`へ継承されることを確認する。
手順:

1. 話者Aに白文字テンプレートを登録する。
2. 話者Bにピンク縁取りテンプレートを登録する。
3. それぞれ短い音声を登録する。
4. `.exo`を生成する。
5. AviUtlで読み込む。
   期待結果: 話者A/Bの字幕がそれぞれ異なる見た目で表示される。

### TEST-002: 字幕長制御

目的: 話者別の字幕長指定が反映されることを確認する。
条件:

* 話者A: 18文字×2行
* 話者B: 10文字×1行
  期待結果: 話者Aは最大36文字相当、話者Bは最大10文字相当で分割される。

### TEST-003: フレーム変換

目的: 秒単位の開始終了時刻がfpsに応じて正しくフレーム化されることを確認する。
条件:

* fps=60
* start_sec=1.0
* end_sec=2.0
  期待結果:
* start_frame=61
* end_frame=120

### TEST-004: 異常系

項目:

* 音声ファイル未登録
* `.exo`未登録
* `.exo`内に「字幕テスト」がない
* 字幕長設定が範囲外
* 出力先に書き込み権限がない
  期待結果: 各エラーIDが返る。

## 9. ADR

### ADR-001: 話者分離を初期スコープ外にする

状態: accepted
決定: 初期版では話者分離を実装しない。
理由: 音声が話者ごとに分離済みであるため、話者分離を入れると不要な複雑性が増える。
影響: 音声認識結果は入力音声に対応する話者IDへ固定割当する。
代替案: WhisperXやpyannote.audioによる話者分離。
不採用理由: 初期目的に対して検証負荷が大きい。

### ADR-002: EXOはテンプレート複製方式で生成する

状態: accepted
決定: `.exo`を仕様から完全生成せず、サンプル`.exo`の対象オブジェクトを複製し、本文と時間だけ差し替える。
理由: AviUtl側の未知パラメータや追加効果を保持できるため。
影響: サンプル`.exo`の品質が出力品質に直結する。
代替案: 独自に全パラメータを構築する。
不採用理由: AviUtl固有仕様の再現範囲が広く、初期MVPに対して過剰。

### ADR-003: ローカルWebアプリを初期形態にする

状態: accepted
決定: 初期版はローカルWebアプリとPythonバックエンドで構成する。
理由: UIを作りやすく、音声処理ライブラリと`.exo`ファイル処理を扱いやすいため。
影響: 実行にはローカル環境構築が必要。
代替案: 完全ブラウザ版、デスクトップGUI版。
不採用理由: 完全ブラウザ版は音声認識負荷が高く、デスクトップGUI版はUI拡張性が低い。

【補足】
この仕様で重要なのは、話者分離を切り捨てたことで構造がかなり単純になる点です。各音声ファイルは最初から話者IDを持つため、AIがやるべきことは「その音声を文字起こしする」「その話者の字幕ルールで分割する」「その話者の`.exo`テンプレートへ流す」の3つに絞られる。話者ごとの差分はコード分岐ではなく、サンプル`.exo`と設定JSONに逃がすため、後から話者C/Dを追加しても実装本体を増やさずに済む。

【索引】
MVP: 最小限の機能で目的検証を行う初期実装単位。
ADR: 設計上の意思決定と理由を記録する文書形式。
ASR: Automatic Speech Recognition、音声認識処理の総称。
EXO: AviUtl拡張編集のオブジェクト情報を保存する形式。
テンプレート複製方式: 既存雛形を保持し必要箇所のみ差し替える生成方式。
責務分離: 変更理由の異なる処理を別単位へ分ける設計原則。
データ駆動: 差分や設定をコードではなく外部データで管理する設計手法。
